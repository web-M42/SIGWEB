import express, { Router } from "express";

import multer     from 'multer';
//import requireDir from 'require-dir';

//Carrega todos os models e deixa público
//requireDir('./models');


// Chama os controllers
//const usuario      = require('./controllers/cartaoController');


const rota : any = Router();


//rota.post('/login', usuario.login);



export default rota;